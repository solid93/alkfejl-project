{% extends 'master' %}

{% block content %}

  <h1>Feladatok</h1>

  {% for task in tasks %}
    <h3>{{ task.title }}</h3>
    <p>{{ task.content }}</p>
  {% else %}
    <h3>Nincsenek feladatok.</h3>
  {% endfor %}

{% endblock %}